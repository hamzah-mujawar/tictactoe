#include <iostream>

int main() {
  /*
   * Need to somehow create two players X and O
   * I assume they will be constants
   */

  // Need to define a board that handles the changes

  // Need to find a way to update the board with a turn

  // Need to find a way to handle turns

  // Need to
}
